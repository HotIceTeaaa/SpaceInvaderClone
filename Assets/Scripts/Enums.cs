public enum BulletType {
    Player,
    Enemy,
}
public enum EnemyType {
    Shooter,
    Bomber,
    Laserer
}

public enum ShipType
{
    Grape,
    Peach,
    Dates,
}

public enum SFX
{
    ButtonClick,
    ButtonHover,
    PlayerDeath,
    PlayerShoot,
    EnemyDeath,
    ProjectileHit,
    ScoreIncrements
}
